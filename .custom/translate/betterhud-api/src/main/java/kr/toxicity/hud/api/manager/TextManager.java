package kr.toxicity.hud.api.manager;

/**
 * Marker interface for the text manager.
 * Manages bitmap/TTF font registration and character-width lookup tables used
 * when calculating the pixel widths of rendered HUD text.
 */
public interface TextManager {
    // Marker – full implementation is in the core module.
}
